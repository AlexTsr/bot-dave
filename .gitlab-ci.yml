production:
  type: deploy
  script:
  - apt-get update -qy
  - apt-get install -y ruby-dev
  - gem install dpl
  - dpl --provider=heroku --app=fathomless-springs-80331 --api-key=$HEROKU_API_KEY
  only:
  - master
